{
  "skill_name": "goal-queue-workflow",
  "evals": [
    {
      "id": 1,
      "name": "enqueue-two-requirements-tonight",
      "prompt": "我下班前想把两个需求排进任务队列今晚自己跑：① 修复导出 CSV 出现重复行的问题；② 给设置页加一个深色模式开关。跑完我回来验收。",
      "expected_output": "应把两个需求各加工成一张含可验证目标条件、预算子句与粗粒度耗时预估的任务卡片写入 .goal-queue/，与人确认一次条件+预算后入库留痕；预估仅供排队参考、允许偏差；应说明定时拉起由平台原生调度能力承担、skill 不内置调度器；除非用户同时要求立即开跑，否则不应启动任何长跑或修改业务代码。",
      "files": [],
      "assertions": [
        {"text": "为每个需求生成一张含可测量目标条件与预算子句的任务卡片，落盘 .goal-queue/"},
        {"text": "为每张卡片给出粗粒度耗时预估，并明示仅供参考（偏差不影响已批准的预算与止损）"},
        {"text": "入库前与人确认一次目标条件与预算，并把确认记录留痕在卡片中"},
        {"text": "说明执行时点由平台原生定时/手动触发承担，不把 cron 等具体命令写成必需路径"},
        {"text": "不启动长跑、不改业务代码（除非用户在同句中明确要求立即开跑）"}
      ]
    },
    {
      "id": 2,
      "name": "stale-card-parked-on-run",
      "prompt": "我的 .goal-queue/ 里有一张卡片要重构 payments-service 的退款接口校验逻辑，但那个模块昨晚刚被同事整体重合并进了 main（旧接口已删除）。刚才我说「跑队列」，现在该怎么办？",
      "expected_output": "消费入口检查应发现卡片约束已失效：冻结的预审批条件不再成立，应将该卡片标记为 conflict pending confirmation 并说明需人重新确认后才能跑，而不是带着过期条件直接启动引擎，也不是让整条队列停摆——其余合法 pending 任务仍按序继续消费。",
      "files": [],
      "assertions": [
        {"text": "识别为条件失效：不在未重新审批的情况下启动该任务的引擎调用"},
        {"text": "将该卡片标记为 conflict pending confirmation 并留待人工重新确认"},
        {"text": "不影响队列里其他合法 pending 任务的继续串行消费"}
      ]
    },
    {
      "id": 3,
      "name": "loaded-without-request-stays-idle",
      "prompt": "你装了 goal-queue-workflow 吧？先帮我看看现在队列里都有些什么，别跑任何东西，我就想知道现状。",
      "expected_output": "加载≠消费：应只输出队列概览（pending/in-progress 计数、剩余任务预估总耗时、下一张可执行卡片），等待明确的队列操作指令，不派发任何任务、不改任何文件状态。",
      "files": [],
      "assertions": [
        {"text": "只输出队列概览信息，不开始消费循环"},
        {"text": "概览包含剩余 pending 任务的预估总耗时"},
        {"text": "明确停在等待指令状态，不主动建议立即开跑并推进"}
      ]
    },
    {
      "id": 4,
      "name": "auto-batch-cap-and-nonblocking-failure",
      "prompt": "自动跑队列，最多跑 3 个任务。提醒一下：第一张卡片的验收条件依赖的测试环境最近不太稳。",
      "expected_output": "应以自动模式逐个串行委派 goal-driven-workflow（模式传播显式声明），单任务因环境问题失败须标记 failed 及原因后继续后续任务；到 3 个任务上限即优雅收尾，剩余保持 pending；结束时产出进度文档+各任务引擎报告+待审查分支清单组成的验收包，停在人工处置处不自行合并。",
      "files": [],
      "assertions": [
        {"text": "子任务以自动模式发起且编排层显式传递模式（非隐式继承）"},
        {"text": "单任务失败标记 failed 并继续后续任务，不阻塞队列"},
        {"text": "到达 max-tasks 上限即停止派发，剩余任务保留 pending"},
        {"text": "产出验收包（进度文档 + 引擎报告 + 分支清单），合并且仅由人决策，AI 不自行 merge"}
      ]
    }
  ]
}
