# Tasks: ai-proxy rename

- [x] 1.1 Skill dir renamed (git mv) + full-repo terminology sweep (values, skill name, proxy-made, section titles, triggers) + version bumps ×6
- [x] 1.2 Spec deltas generated from live specs (goal-queue ×4 MODIFIED + 1 RENAMED; goal-run ×1 RENAMED + MODIFIED; ai-counterpart REMOVED; ai-proxy ADDED)
- [x] 1.3 Legacy-value mapping lines in batch/workflow references
- [x] 1.4 AGENTS.md rows; evals swept; index regenerated
- [x] 1.5 lint --staged; openspec validate; residue grep zero (old terms repo-wide); archive; commit; push; PR
