# goal-queue-concurrent-throughput

goal-driven-queue concurrent consumption: slot-based dispatch with configurable concurrency (enqueue-time decision, absent-runs reuse the recorded value), worktree-mandatory isolation when concurrency >= 2, same-module exploration amortization with write serialization, acceptance merge-decision compression
