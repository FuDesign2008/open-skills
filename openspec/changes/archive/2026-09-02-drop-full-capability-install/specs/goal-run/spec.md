## REMOVED Requirements

### Requirement: Goal-run command entry SHALL follow command-file conventions when present

**Reason:** The repository no longer ships user-facing slash commands, including `commands/goal-run.md`.

**Migration:** Invoke `goal-driven-workflow` via its documented triggers (e.g. 「目标驱动」) or by naming the skill.
