## REMOVED Requirements

### Requirement: Nine disciplines with detection methods

Removed: the nine disciplines and their detection methods move into `perf-optimize-workflow`'s in-file Evidence validity disciplines section (capability consolidated; single consumer).

### Requirement: Gate rule — decision-grade vs trend-only

Removed: the gate rule is carried verbatim into the host's in-file section and its spec requirement (Evidence validity disciplines SHALL be owned in-file).

### Requirement: Mount via host dependency, no reverse dependency

Removed: obsolete — with the disciplines in-file there is no mount relationship to express.

### Requirement: Platform-agnostic rules, concrete cases in reference

Removed: carried into the host section and reference Part 4; the platform-agnostic wording requirement survives there.
